#include "Log.h"
#include "Program.h"

void prog_setup() {
  
}


void prog_loop() {

	// Scan for cards
	// If a card is found, read it
	// Check if card is valid through DB
	// Open door if the card is valid, otherwise, flash red LED

} 
